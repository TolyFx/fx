name: fx_framework
description: "Fx 框架统一集成入口，一个依赖引入全部核心能力。"
version: 0.1.0
homepage: https://github.com/TolyFx/fx

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  fx_exception: ^0.0.1+3
  fx_env: ^0.0.1+4
  fx_event: ^0.0.1
  fx_trace: ^0.1.0
  fx_dio: ^0.0.5+2
  fx_boot_starter: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
