name: fx_media_picker
description: "Default MediaPicker implementation for fx_media. Wraps platform-specific picker libraries."
version: 0.0.1
homepage: https://github.com/TolyFx/fx

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  fx_media: ^0.0.1
  image_picker: ^1.1.2
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
