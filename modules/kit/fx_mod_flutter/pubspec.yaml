name: fx_mod_flutter
description: "Flutter assembly adapter for fx_mod modules, localization and widget scopes."
version: 0.1.0
homepage: https://github.com/TolyFx/fx
repository: https://github.com/TolyFx/fx/tree/main/modules/kit/fx_mod_flutter

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  fx_exception: ^0.0.1+4
  fx_mod: ^0.1.0
  go_router: ^14.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  fx_exception:
    path: ../../core/fx_exception
  fx_mod:
    path: ../../core/fx_mod

flutter:
